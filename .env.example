# Copy this file to .env and fill in your value.
# .env is gitignored — your secrets stay local.
#
# On Render, set DATABASE_URL in the dashboard under Environment Variables
# (or use render.yaml which wires it automatically).
#
# Leave unset to use the default local SQLite database.
# DATABASE_URL=postgresql://user:password@host:5432/grocery_delivery
